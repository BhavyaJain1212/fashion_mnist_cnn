Flask==3.1.2
gunicorn==23.0.0
numpy==2.2.6
pillow==12.0.0

# CPU-only PyTorch wheels (prevents downloading CUDA / nvidia-* packages)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.9.1+cpu
torchvision==0.24.1+cpu
